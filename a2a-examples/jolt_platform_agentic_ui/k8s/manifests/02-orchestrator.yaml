apiVersion: apps/v1
kind: Deployment
metadata:
  name: jolt-orchestrator
  namespace: jolt-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jolt-orchestrator
  template:
    metadata:
      labels:
        app: jolt-orchestrator
    spec:
      containers:
      - name: jolt-orchestrator
        image: jolt-orchestrator:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8088
        envFrom:
        - configMapRef:
            name: jolt-platform-config
        - secretRef:
            name: jolt-secrets
        resources:
          requests:
            cpu: "500m"
            memory: "1Gi"
          limits:
            cpu: "1"
            memory: "2Gi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8088
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: jolt-orchestrator-service
  namespace: jolt-platform
spec:
  selector:
    app: jolt-orchestrator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8088
  type: ClusterIP
